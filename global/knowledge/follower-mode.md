# Follower Mode

Another session holds the project lock. Run `EnterWorktree` NOW — before any file writes, including session bookkeeping. Then work normally (full session protocol applies on the branch).
