<!-- Action: act -->
<!-- Tracked-by: AFT-58, AFT-59, AFT-60, AFT-61 -->
# Inbox intake — 2026-08-24

Delivered by a cfg-agent-fleet session while agent-fleet had no active session, under the
corrected cross-project rule (the four system projects may write directly to any project
whose session is not active). These items were removed from `cross-project/inbox.md`.

**Why this file and not just `backlog.md`:** agent-fleet gitignores `backlog.md` (`.gitignore:76`),
so a backlog-only delivery would be invisible on every other machine. The backlog carries
four short pointers; the content lives here, tracked and synced.


- [ ] [P1] **AFT-58: Three propagation items, one of them a live defect in agent-fleet own test runner.** **(1) YOUR `setup/tests/run.sh` IS DESTRUCTIVE — fix this before running your suite again.** cfg's `run.sh:45-46` skips `test-e2e-*` unless `--e2e` is passed, *because those spawn real Claude Code sessions*; your copy lost that guard, so they run by default. Proven, not theorised: running your suite this session left `global/foundation/user-profile.md` modified in your live working tree with a fictional onboarding profile written by `test-e2e-onboarding.sh`, which hardcodes `AF_DIR="$HOME/agent-fleet"` at line 14. It was reverted. Related and worse in cfg: `test-e2e-daily-workflow.sh:88` calls `rotate-session.sh` with NO directory argument while `rotate-session.sh:32` defaults `PROJECT_DIR` to `.`, so it rotates whatever live repo the suite runs from — that blanked a live `session-context.md` twice in one session. Port the `--e2e` guard, and point the E2E fixtures at a temp clone rather than `$HOME/agent-fleet`. **(2) Nine Cat-3 hook files now have manifest rows but still need human genericization before they can ship** — they are flagged, never auto-copied, so nothing is broken while they wait: `afk-deactivate.sh` (already scrubbed by cfg — do not regress it), `gmail-search-guard.sh` (names private correspondents), `point-of-action-knowledge.sh` (`mcp__github-ivoclar__*` tool keys), `vault-read-guard.sh` (employer vault), `checks/03-inbox-services.sh` (private project names in comments), `checks/06c-conversation-log-gap.sh` (one comment), `checks/07b-platform-env.sh` (a persona name in a script filename — a rename is the only blocker), `checks/19-scheduled-tasks.sh` (real hostnames + a username; needs a config overlay). **⚠ `checks/06a-session-state.sh` is the structural one:** it is the RENAMED successor of your `06-state-injection.sh`, and `config-check.sh` globs `checks/*.sh`, so it must land WITH a `git rm` of `06-state-injection.sh` in the SAME commit or you double-inject HOSTNAME/PERSONA/HANDOFF/PENDING_FILES. Also worth checking while you are in there: your `06-state-injection.sh` and `06b-lock-knowledge.sh` appear to overlap on PROJECT_KNOWLEDGE/SIBLING_SESSION already, independent of this work. **(3) Two files were recategorized to flag-only and MUST stay that way:** `global/knowledge/fleet-capabilities.md` and `global/reference/backlog-convention.md`. Both were scrubbed of personal content in your repo; `fleet-capabilities.md` was previously "Must Be Identical", which meant the next `template-push` would have copied cfg's version straight back over the scrub and re-published it. Do not promote either back to Cat-1. Source: cfg-agent-fleet WSL 2026-08-11.

- [ ] [P1] **AFT-59: Consolidated review/port task replacing 14 earlier inbox items (2026-04-01 to 2026-08-11).** **One agent-fleet session should do the whole Cat-3 review + hook-port backlog.** (a) **The 23 Category-3 flag-only template files need the manual review pass (CFG-493)** — `sync.sh template-push` never auto-copies them; blocked behind it are cfg's seven generic knowledge files (`user-roles-and-personas`, `needs-to-requirements`, `vertical-slicing`, `deliverable-authoring`, `presentation-design`, `project-economics`, `office-templates`), their `global/CLAUDE.md` loading rows, and `setup/templates/{deliverable-set,office}/`. (b) **Per-file diff reasons from the auto-detect rows (CFG-432/497/499):** `responsibility-modeling.md` (personal name + `ivoclar/regulatory/` source path), `glib-idle-callback-pattern.md` (p0rn PRN-220 / Steam Deck attribution), `gmail-search-guard.sh` (private correspondents in header), `point-of-action-knowledge.sh` (`mcp__github-ivoclar__*` tool keys), `vault-read-guard.sh` (employer vault named throughout), `checks/03-inbox-services.sh` (private project names in CFG-483 comments + tag examples), `checks/06c-conversation-log-gap.sh` (one private-project comment), `checks/19-scheduled-tasks.sh` (real hostnames + username — needs a config overlay), and ⚠ **`checks/06a-session-state.sh` is STRUCTURAL**: it is the renamed successor of the template's `06-state-injection.sh`, and `config-check.sh` globs `checks/*.sh`, so it must land WITH `git rm` of `06-state-injection.sh` in the SAME commit or HOSTNAME/PERSONA/HANDOFF/PENDING_FILES double-inject (also check the pre-existing 06-state-injection/06b-lock-knowledge PROJECT_KNOWLEDGE/SIBLING_SESSION overlap). (c) **Hook-port registrations still missing from `setup/config/settings.json` (Cat-3, manual):** `vault-read-guard.sh` and `manifest-push-check.sh` (hook shipped in CFG-393 commit `d44f787`) each need the standard safe-run PreToolUse block — `{"matcher":"Bash","hooks":[{"type":"command","command":"bash ~/.claude/hooks/safe-run.sh <hook>.sh"}]}` — plus optional copies of `setup/tests/test-vault-read-guard.sh` (17 tests) and `test-manifest-push-check.sh` (10 tests, both excluded from template-push). (d) **Port the CFG-391 Phase 0.8 `--push` preflight** from `~/cfg-agent-fleet/global/hooks/config-auto-sync.sh:139-188` (search "CFG-391") into the template's `config-auto-sync.sh`, keeping its `_detect_config_repo()` path resolution. (e) **`~/agent-fleet/CLAUDE.md` still lacks Active Roster / Reference / Project Structure sections** (triage 2026-04-01, verified 2026-08-21). (f) **Two files were recategorized flag-only and MUST stay that way after their d0fca9a scrub:** `global/knowledge/fleet-capabilities.md` and `global/reference/backlog-convention.md` — do not promote back to Cat-1 or the next template-push republishes the scrubbed content. (Already DONE per commits `d0fca9a`+`982ce72`: `afk-deactivate.sh`, `checks/07b-platform-env.sh`, and the two scrubs themselves — archived.) Note: the eight `CFG-534` Cat-3 rows filed 2026-08-21 are separate newer items below and fold naturally into the same session. Sources: cfg-agent-fleet 2026-04-26/2026-06-03/2026-06-17/2026-08-07/2026-08-11; auto-detect 2026-08-09/11.

- [ ] [P2] **AFT-60: Category-3 template review backlog — 8 files flagged by `template-push`, each needing a manual sanitisation pass.** cfg cannot auto-copy these: they carry personal paths or name private projects. The flags as filed:
  - Cat-3 review — `global/foundation/cross-project-sync.md`. Diff: Shared-strategy pattern for coordinating overlapping projects. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `global/foundation/first-run-refinement.md`. Diff: Post-setup interactive personalization protocol (`.setup-pending` trigger). Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `global/foundation/protocol-creation.md`. Diff: When/how to create new domain protocols. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `global/foundation/session-shutdown.md`. Diff: The 8-step shutdown checklist. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `setup/scripts/ask-passphrase.sh`. Diff: Cross-platform masked passphrase dialog (tkinter/zenity/kdialog/PowerShell WPF/`read -s`). Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `setup/scripts/cc-update.sh`. Diff: Updates Claude Code inside the cc-mirror, refuses to run inside a CC session. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `setup/scripts/e2e-podman.sh`. Diff: Runs E2E deployment tests in a rootless Podman container. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.
  - Cat-3 review — `setup/scripts/sched-lib.sh`. Diff: Scheduled-task library (registration, date-gating, scope matching) sourced by check 19. Propagates with path sanitization. Row added 2026-08-21 (CFG-534).. Source: auto-detect 2026-08-21.

- [ ] [P2] **AFT-61: `03-inbox-services.sh` needs a manual Category-3 sanitisation pass for the new CFG-134 child-inbox check.** **`global/hooks/checks/03-inbox-services.sh` gained a new Check 3.2c (`CFG-134`) that cfg cannot auto-copy to you**, because the manifest classes this file **Cat-3 / flag-only** — its existing `CFG-483` comments name a private project incident twice and the tag-form examples name three more private projects, so `template-push` flags it and never copies. That classification is correct and should stay; this item is the manual pass it implies.

