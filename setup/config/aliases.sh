#!/usr/bin/env bash
# Shared shell aliases — deployed by cfg-agent-fleet sync.sh to ~/.cfg-aliases.sh
# Machine-specific aliases stay in machine-local files (e.g., ~/infrastructure/workstation/wsl/aliases.sh)

# DOS-style shortcuts
alias cls='clear'
alias md='mkdir -p'
